import React from "react";
const SectionTitle = ({ title }) => {
  return (
    <div className="w-full py-2 font-semibold border-b-2 border-gray-100">
      {title}
    </div>
  );
};
export default SectionTitle;
